{
    "mcpServers": {
        "visual-layer": {
            "command": "/Users/Jack/.local/bin/uv",
            "args": [
                "--directory",
                "/Users/Jack/Documents/vl-mcp/mcp-server",
                "run",
                "mcp_server/server.py"
            ],
            "env": {
                "VISUAL_LAYER_API_KEY": "${VISUAL_LAYER_API_KEY}",
                "VISUAL_LAYER_API_SECRET": "${VISUAL_LAYER_API_SECRET}"
            }
        }
    }
}